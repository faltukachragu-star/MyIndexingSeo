# For the FastAPI web framework
fastapi==0.111.0

# The server that runs FastAPI (ASGI server)
uvicorn==0.30.1

# The high-performance, asynchronous HTTP client for API calls
aiohttp==3.9.5

# The modern, official library for handling Google authentication
google-auth==2.30.0

# Required by FastAPI for handling file uploads from HTML forms
python-multipart==0.0.9
